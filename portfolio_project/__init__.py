"""
Initializes the 'portfolio_project' directory as a Python package.

This package is the main configuration root for the Django project,
containing settings, root URLs, and server entry points (WSGI/ASGI).
"""